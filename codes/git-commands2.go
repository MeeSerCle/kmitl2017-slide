//git show remote
git remote show

// Add remote repository
git remote add <name> <git-url>

// Push change set to remote
git push <remote-name> <branch>

// Pull change from remote
git pull <remote-name> <branch>

// check difference with Git pointer
git diff <pointer>
git diff HEAD
git diff --staged

// Reset change
git reset <file>

// Checkout 
git checkout -- <target>